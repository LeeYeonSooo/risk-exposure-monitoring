# Agent Handoff

Start with `docs/eoa-flow-agent-handoff.md` when working on the current
`grahahahahahahaha` branch. It explains why this repo is paired with
`/Users/link/defi-dagggg`, what was built during the EOA-flow work, and which
commands verify the current state.

Do not print API keys. Local DeFi keys are expected to be loaded from
`/Users/link/podotree/.env` or process env.
